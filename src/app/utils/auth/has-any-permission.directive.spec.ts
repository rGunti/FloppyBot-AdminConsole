import { HasAnyPermissionDirective } from './has-any-permission.directive';

describe('HasAnyRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new HasAnyPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
