import { ChannelNamePipe } from './channel-name.pipe';

describe('ChannelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
